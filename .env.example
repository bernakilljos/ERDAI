# ===== Frontend (Vite) =====
VITE_API_URL=http://localhost:4000

# ===== App =====
APP_ENV=local
APP_PORT=3000
API_PORT=4000
PY_WORKER_PORT=8000
PY_WORKER_URL=http://localhost:8000
# Python Worker 구동: cd worker && uvicorn app.main:app --port 8000 --reload
# WORKER_STUB=true이면 Python Worker 없이 stub 응답 (개발 초기 사용)
WORKER_STUB=true

# ===== JSON 저장소 =====
# data/ 폴더에 프로젝트별 JSON 저장

# ===== Default Dev DB Connection (사용자 연결 대상, 예시) =====
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_PORT=13306
DB_NAME=rms_dev
DB_USER=rms_test
DB_PASSWORD=test1234

# ===== Optional: Encrypt DB credentials at rest =====
DB_CRED_ENCRYPTION_KEY=CHANGE_ME_32BYTE
